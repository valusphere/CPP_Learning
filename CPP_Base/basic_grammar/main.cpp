#include <iostream>

// 입출력은
// 프로그램과 외부 세계가 소통하는 통로이다.

// 1. 손님이 주문을 합니다 -> 입력
// 2. 주방에서 음료를 만듭니다 -> 가공(처리)
// 3. 완성된 음료가 나옵니다 -> 출력

// std::cin -> 입력
// std::cout -> 출력

// std는 표준 라이브러리 이름이고
// ::는 범위를 지정해주는 연산자이다

//입력 → 가공(처리) → 출력

int main(void)
{
	int x;

	//printf("값은 %d 입니다\n", x);
	//std::cout << "값은 " << x << " 입니다\n";
	std::cout << "정수를 입력하세요: "; 
	std::cin >> x;

	std::cout << "입력한 값은 " << x << " 입니다.\n";
	return 0;
}